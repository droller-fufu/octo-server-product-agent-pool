# 已迁移：存储与外部依赖

本文件是旧占位入口，避免误点空壳。正式考试答题请使用：

- [`knowledge/storage-dependencies.md`](./storage-dependencies.md)

用途说明：`storage-dependencies.md` 覆盖 MySQL、Redis/TLS、migration、限流/会话、App Bot registry、对象存储预签名、外部部署依赖边界等考点。
